import 'package:flutter/material.dart';

class DeepLinkDetailScreen extends StatelessWidget {
  const DeepLinkDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("DEEPLINK DETAIL"),
      ),
    );
  }
}
